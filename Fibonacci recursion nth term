def fib(n):
    a=1
    b=1
    if n==1:
        return(a)
    if n==2:
        return(b)
    else:
        return(fib(n-1)+fib(n-2))

x=int(input("Enter no of fibonacci terms: "))
print(fib(x))
    
